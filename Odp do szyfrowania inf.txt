a) 
1. AKRA
2. MTROO
3. AGRTIALOYMK

b)
Zmienna k

c)
    while (i < s.size())
    {
        w = w + s[i]; // w ← w ∙ s(i)
        w = w + s[i-1]; // w ← w ∙ s(i-1)
        i = i + 2; // i ← i + 2
        if (i == s.size()) // jeśli i = długość (s)
        {
            w = w + s[i-1]; // to w ← w + s(s-1)
        }
    }