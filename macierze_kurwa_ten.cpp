#include <bits/stdc++.h>

using namespace std;

int main()
{
    int i, j, temp = 0;
    int Tab[i][j];
    for (int w = 0; w < i; w++)
    {
        for (int z = 0; z < j; z++)
        {
            cout << "Podaj wartosc w indeksie:";
            cin >> temp;
            Tab[w][j] = temp;
        }
    }
    for (int w = 0; w < i; w++)
    {
        for (int z = 0; z < j; z++)
        {
           if (Tab[w][z] + )
        }
    }
}